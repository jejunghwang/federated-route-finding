# 2026-05-04

## graph_shortest_path_spec_v2.md — Floyd-Warshall patch 섹션 추가

### Before
spec 본문이 BFS (`nx.shortest_path`) 기반으로 작성되어 있었으나, 실제 구현(`src/pangpang_pathfinder/route/graph.py`, `src/pangpang_pathfinder/route/planner.py`)은 이미 Floyd-Warshall로 전환되어 있어 문서와 코드 간 괴리.

### After
spec 끝에 `## Patch — Floyd-Warshall로 전환 (2026-05-04)` 섹션 추가:
- 동기 (BFS와 결과는 동일하지만 startup precompute / edge weight 일반화 / distance matrix 자산화 / 발표 변별력)
- 변경점 코드 스니펫: `__init__`에서 `nx.floyd_warshall_predecessor_and_distance`, `plan_route`에서 `nx.reconstruct_path` 사용
- 호환성 명시 (`plan_route` 시그니처 / `Route` dataclass / 기존 테스트 통과)

### 검증
- `pytest tests/test_graph.py -v` → 7/7 통과

## E2E 검증 (Playwright)

`utils/app.py` 백그라운드 실행 → `http://127.0.0.1:7860` 접속 → Scene B 탭에서 검증:
- **드롭다운**: configs/graph.yaml의 15개 노드명 모두 로드 확인
- **이미지 업로드**: 224x224 단색 PNG → 현재 위치 사진
- **목적지 선택**: "100주년기념관 2층 로비"
- **결과 경로**: 비마관 101호 앞 → 비마관 2층 복도 → 중앙광장 → 100주년기념관 동문 연결로 → 100주년기념관 2층 로비 (4 hops, F-W reconstruct_path)
- **콘솔 에러**: 0

graph.yaml의 component 구조 (3개 컴포넌트로 분리됨):
- main: bima_*, chambit_*, centennial_*, outdoor_*, hwado_*
- saebit: saebit_101_front ↔ saebit_office
- okui: okui_3f_corridor ↔ okui_elevator

## 폴더 구조 정리

남의 코드(`src/`, `docs/`, `configs/`, `scripts/`, `tests/`)는 최소 터치 원칙. `utils/`(본인 폴더)와 명백한 leftover만 정리.

### 변경

```
파일/폴더                                              변경                                       이유
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────
scene_b_route_result.png                               삭제                                       playwright 검증 leftover
src/pangpang_pathfinder.egg-info/                      삭제                                       pip install -e . 빌드 산출물
utils/assets/                                          삭제                                       빈 폴더 (.gitkeep만 존재)
utils/core/__pycache__/, utils/ui/__pycache__/         삭제                                       dev cache
.playwright-mcp/                                       삭제                                       playwright 세션 상태
.gitignore                                             + *.egg-info/, + .playwright-mcp/          빌드/도구 산출물 추적 방지
```

### `utils/core/stitcher.py` — cwd 의존 버그 회피

**Before**: `_stitch_clips(edges)` 호출 시 `src/.../route/stitching.py`의 `ROUTE_CLIPS_DIR = Path("data/route_clips")` (상대경로)가 cwd 의존이라, `python utils/app.py`를 `utils/`에서 실행하면 `utils/data/route_clips/`를 찾아 영원히 못 매칭.

**After**: utils/ 안에서 `PROJECT_ROOT = Path(__file__).resolve().parents[2]` 기준 절대경로 계산 후 `_stitch_clips(edges, clips_dir=ROUTE_CLIPS_DIR)`로 명시 전달. 남의 파일(`src/.../route/stitching.py`) 안 건드림.

검증: `os.chdir('utils')` 후에도 `ROUTE_CLIPS_DIR == C:\Users\minjae\federated-route-finding\data\route_clips` 유지 확인.

### `data/README.md` — Route clips 섹션 append

기존 데이터 layout 설명 뒤에 한 섹션 추가:
- `data/route_clips/<from_id>__<to_id>.mp4` 경로 컨벤션
- node id는 `configs/graph.yaml` snake_case
- 무방향이라 한 방향만 두면 reverse 자동 매칭

## README 업데이트 (다른 사람을 위한 변경사항 명시)

다른 팀원이 본 브랜치 변경을 알 수 있도록 README 3건 갱신.

### `utils/README.md` — 전면 재작성

**Before**: "dummy 모듈만 포함. 분류기 / graph / stitcher 는 모두 더미." + `assets/dummy_route.mp4` 언급 (이미 삭제됨)

**After**:
- legacy scaffold 임을 명시, 정식 모듈(`src/pangpang_pathfinder/`)과의 wrapper 관계 표
- F-W graph/planner 사용 명시 ("실제 graph + 최단경로 연결 [x]")
- node_id 기반 파이프라인 설명 (이름 아님)
- 영상 cwd-independent 절대경로 동작 명시
- placeholder stitcher의 1-clip 한계 명시

### 루트 `README.md` — 폴더 구조 + 변경사항 섹션

- 폴더 트리에 `utils/`, `_docs/` 추가
- "Gradio 데모 실행"에 `python utils/app.py` 옵션 추가
- 신규 섹션 `## 🔄 본 브랜치 주요 변경` — F-W 도입, utils wrapper 통일, route_clips 컨벤션, 폴더 정리, 검증 결과 5블록

### `_docs/README.md` — 신규

폴더 의도 설명 (작업 일지, `docs/`와 분리). 다른 사람이 `_docs/` 보고 헷갈리지 않게.

### 추가 README 5건 신규

```
파일                                          내용
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
configs/README.md                             yaml 5종(graph/classes/clients/train/federated) 스키마 + 추가 시 일관성 규칙 (id ≡ slug ≡ 폴더명)
scripts/README.md                             6 CLI 실행 순서 (prepare → train_local x4 → merge → evaluate → predict_route / launch_demo) + 입출력 표
src/pangpang_pathfinder/README.md             패키지 트리 + 모듈별 진입점
src/pangpang_pathfinder/route/README.md       F-W 설계 (BFS 비교, 선택 이유 4가지), 노드 추가 예시, API 시그니처, stitching 한계
tests/README.md                               pytest 실행, 파일별 범위, 새 테스트 추가 가이드
outputs/README.md                             checkpoints/reports 구조, gitignore 패턴, 재현 절차
```

총 README 10개 (root + 9개 폴더).
